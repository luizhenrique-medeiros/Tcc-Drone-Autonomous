name: drone_delivery_mobile
description: Aplicativo Android do cliente para entregas acadêmicas por drone.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  geolocator: 14.0.3
  google_maps_flutter: 2.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/devcore-logo-source.png
